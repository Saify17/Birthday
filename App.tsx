/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 *
 * NOTE: The main interactive birthday experience lives in `index.html`
 * (vanilla HTML + JS + Tailwind). This React entry is kept as a minimal
 * placeholder so the project structure mirrors the original repo.
 *
 * The deployed / hosted version is `index.html`. To open it, just run a
 * static server (e.g. `python3 -m http.server`) and visit the page.
 */
export default function App() {
  return null;
}
